generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Movement {
  id        Int      @id @default(autoincrement())
  code      String
  item      String
  sector    String
  status     String
  createdAt DateTime @default(now())
}